[tools]
stylua = "latest"
"aqua:Kampfkarren/selene" = "latest"

[tasks.validate]
description = "Validate formatting (StyLua), linting (Selene), and complexity (Luacheck <= 10)"
run = """
stylua --check .
selene .
luacheck .
"""
