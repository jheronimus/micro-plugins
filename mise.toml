[tools]
stylua = "latest"
"aqua:Kampfkarren/selene" = "latest"

[tasks.validate]
description = "Validate formatting (StyLua), linting (Selene), and complexity (Lizard <= 10)"
run = """
stylua --check .
selene .
uvx lizard -C 10 -w --exclude docs .
"""
